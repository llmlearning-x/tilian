# HTTP 协议基础

HTTP（HyperText Transfer Protocol）是互联网上应用最为广泛的一种网络协议。

## 默认端口

HTTP 协议默认使用的端口号是 80。当 URL 中没有指定端口时，浏览器会默认使用 80 端口。

## HTTPS

HTTPS 是 HTTP 的安全版本，默认使用 443 端口。

## 请求方法

常见的 HTTP 请求方法包括 GET、POST、PUT、DELETE 等。
